git init  // Para Iniciar Git
commint -m // Para Confirmar los Cambios 
Cat "Nombre del archivo" // para leer archivo
git add . //para agregar todo 
git show  // Para Mostrar Cambios Realizados
git log // para ver historial de modificaciones
git diff + clave + clave // Se colocan las claves con que se van a comparar
git reset + clave +  hard or soft // para volver a tras sin retorno
git checkout + clave + nombre de archivo // sierve para volver atras con retorno
git branch + "Nombre de la nueva rama"
git merge  + Nombre dela rama con la que vas a unir
git remote origin + url // Crea el enlace entre git and gighob 
git push origin (Nombre de la rama) // envia la rama principal al gighob
git pull origin master --allow-unrelated-histories // forzar primet convinacion desde githob
git config --global user.email "Correo que se usara" // asignar o cambiar Correo
ssh-keygen -t rsa -b 4096 -C "joregesosa@gmail.com" // Crear llave de cifrado
ssh-add /home/jorge/.ssh/id_rsa  // Agregar 
eval $(ssh-agent -s)
git remote set-url origin + nueva Url // Cambiar url del proyecto
git commit -am "nota" // para no tener que hacer add y luego commit
git pull origin "Nonbre de la rama"  // se usa para traer y fucionar todo desde githob a local
git log --all // Mostrar todo las historia del proyecto
git log all -- graph // para ver el mapeo de merge 
git log all -- graph --decorated --oneline // organiza los logs de una manera mas legible
alias + nombre de alias + "Comando" // crea alias para linea de codigos muy largas
git  show-branch --all // Muestra las rammas 
git clone + url // clonar proyecto desde githop
git stash // Guarda en memoria la cambios que aun no han sido agregados o enviados 
git stash branch + "nombre de branch" // Crea una nueva rama y deposita lo guardados con stash en esa rama
git chery-pick + clave // fusiona un commit viejo con el Master 
git log --oneline // muestra cambios con claves abreviadas 
git commit --amend (Deben agregarse los cambios con "git add ." antes de ejecutar este comando) // Remienda un commit